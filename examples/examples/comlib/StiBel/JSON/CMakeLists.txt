#可执行文件输出目录
set(CMAKE_RUNTIME_OUTPUT_DIRECTORY ${COMLIB_EXECUTABLE_OUTPUT_PATH}/JSON)

#头文件目录
INCLUDE_DIRECTORIES( ${STIBEL_JSON_INCLUDE}  ${JSONCPP_INCLUDE} ${STIBEL_COMMON_INCLUDE} ./include )

#目录源码路径
SET( STIBEL_JSON_TEST_SRC_DIR ./source )

#代码列表
FILE (GLOB STIBEL_JSON_TEST_FILES ${STIBEL_JSON_TEST_SRC_DIR}/*.cpp)

#依赖的LIB
SET(STIBEL_JSON_TEST_LIB  ${LIB_STIBEL_JSON} ${LIB_STIBEL_COMMON})

#生成 
ADD_EXECUTABLE( JsonUtilTest ./source/JsonUtilTest.cpp)

#连接生成相关库
TARGET_LINK_LIBRARIES( JsonUtilTest ${STIBEL_JSON_TEST_LIB} )

#生成 
ADD_EXECUTABLE( JSONExceptionTest ./source/JSONExceptionTest.cpp )

#连接生成相关库
TARGET_LINK_LIBRARIES( JSONExceptionTest ${STIBEL_JSON_TEST_LIB} )